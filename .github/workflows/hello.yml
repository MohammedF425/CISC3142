name: Hello World
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - run: echo "Running makefile" 
        - name: create prog
          run: g++ CISC3142/Exer3/hello.cpp
        - run: ./a.out
